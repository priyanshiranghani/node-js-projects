<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>

    <!-- Google Font -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            font-family: 'Poppins', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            background-color: #1a202c; /* Dark gray background */
        }

        .card {
            background: linear-gradient(145deg, #3b82f6, #9333ea); /* Dual-tone gradient */
            padding: 2rem;
            border-radius: 16px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
            max-width: 420px;
            width: 100%;
            color: white;
        }

        .card h2 {
            font-size: 1.75rem;
            font-weight: 600;
            text-align: center;
            margin-bottom: 1.5rem;
        }

        .form-control {
            background: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.4);
            color: white;
            border-radius: 8px;
            padding: 0.75rem 1rem;
        }

        .form-control:focus {
            background: rgba(255, 255, 255, 0.2);
            border-color: #ffafcc;
            box-shadow: 0 0 8px rgba(255, 175, 204, 0.5);
        }

        .btn-primary {
            background: linear-gradient(145deg, #9333ea, #3b82f6); /* Button gradient */
            border: none;
            padding: 0.8rem 1rem;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .btn-primary:hover {
            background: linear-gradient(145deg, #7e22ce, #2563eb); /* Darker gradient */
            transform: scale(1.02); /* Slight zoom on hover */
            transition: all 0.3s ease-in-out;
        }

        .text-sm {
            font-size: 0.875rem;
            text-align: center;
            margin-top: 1rem;
        }

        .text-sm a {
            color: #facc15; /* Bright yellow link */
            text-decoration: none;
        }

        .text-sm a:hover {
            text-decoration: underline;
            color: #fbbf24;
        }
    </style>
</head>
<body>
    <div class="card">
        <h2>Login</h2>
        <form action="/loginuser" method="post" onsubmit="return validateForm()">
            <div class="mb-3">
                <input 
                    type="email" 
                    id="email" 
                    name="email" 
                    class="form-control" 
                    placeholder="Enter Your Email...." 
                    required>
                <small id="emailError" class="form-text text-danger d-none">Invalid email address.</small>
            </div>

            <div class="mb-3">
                <input 
                    type="password" 
                    id="password" 
                    name="password" 
                    class="form-control" 
                    placeholder="Enter Your Pass " 
                    required>
                <small id="passwordError" class="form-text text-danger d-none">Password cannot be empty.</small>
            </div>

            <button type="submit" class="btn btn-primary w-100">Login</button>
        </form>
        <p class="text-sm">Don't have an account? <a href="/res">Register Now</a></p>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
