<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel</title>

     <!-- Bootstrap CSS -->
     <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        /* General Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f6f9;
            color: #333;
            background-image: url('../uploads/blog-image-background.jpg');
            background-repeat: no-repeat;
            background-size: cover;
        }

        a {
            text-decoration: none;
            color: inherit;
        }

        /* Header */
        .header {
            background: #4CAF50;
            color: white;
            padding: 1rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .header h1 {
            font-size: 1.8rem;
            font-weight: bold;
        }

        .header button {
            background-color: #ff5252;
            border: none;
            color: white;
            padding: 0.6rem 1.2rem;
            font-size: 1rem;
            border-radius: 8px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .header button:hover {
            background-color: #e53935;
        }

        /* Sidebar */
        

        .nav-links {
            display: flex;
            
            gap: 1rem;
        }

        .nav-link {
            font-size: 1rem;
            padding: 0.8rem 1rem;
            background: #ffffff;
            border-radius: 6px;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            transition: background-color 0.3s ease;
        }

        .nav-link:hover {
            background-color: #f5eded;
        }

        .nav-link i {
            font-size: 1.2rem;
        }

        /* Main Content */
        .main-content {
            /* margin-left: 250px; */
            padding: 2rem;
        }

        .main-content h2 {
            font-size: 2rem;
            margin-bottom: 1.5rem;
        }

        /* Cards */
        .card {
            background: white;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }

        .card img {
            width: 100%;
            object-fit: cover;
        }

        .card-content {
            padding: 1rem;
        }

        .card-content h3 {
            font-size: 1.5rem;
            color: #4CAF50;
            margin-bottom: 0.8rem;
        }

        .card-content p {
            font-size: 0.9rem;
            color: #777;
        }

        .card-buttons {
            display: flex;
            justify-content: space-between;
            padding: 1rem;
            border-top: 1px solid #f4f4f4;
        }

        .card-buttons a {
            padding: 0.6rem 1rem;
            border-radius: 6px;
            text-align: center;
            font-size: 0.9rem;
            transition: background-color 0.3s ease;
        }

        .card-buttons .btn-edit {
            background-color: #FFD700;
            color: #333;
        }

        .card-buttons .btn-edit:hover {
            background-color: #FFC107;
        }

        .card-buttons .btn-delete {
            background-color: #FF5252;
            color: white;
        }

        .card-buttons .btn-delete:hover {
            background-color: #E53935;
        }

        .card-buttons .btn-view {
            background-color: #4CAF50;
            color: white;
        }

        .card-buttons .btn-view:hover {
            background-color: #45A049;
        }

        /* Grid */
        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 1.5rem;
        }
    </style>
</head>

<body>
   
    <div class="container">
    <!-- Sidebar -->
    <aside class="sidebar">
        <h1 align="center" class="text-black m-3">View Blogs</h1>
        <div class="nav-links justify-content-center" >
            <a href="/view" class="nav-link"><i class="bi bi-view-list"></i> View Blogs</a> 
            <a href="/add" class="nav-link"><i class="bi bi-pencil"></i> Add Blog</a>
            <a href="/logout" class="nav-link"><i class="bi bi-gear"></i> Log out</a>
        </div>
    </aside>

    <!-- Main Content -->
    <main class="main-content">
        
        <div class="content">
       
            

            <div class="row mt-5">
                <% user.map((val, index) => { %>
                    <div class="col-md-4 mb-4">
                        <div class="card shadow">
                            <img src="<%= val.image %>" class="card-img-top img-fluid " alt="Movie Image">
                            <div class="card-body " align="center">
                                <h5 class="card-title"><%= val.name %></h5>
                                <p>
                                    <%= val.description.slice(0, 100) %>...
                                    <a href="/readmore/<%= val.id %>" class="text-primary">Read More</a>+
                                </p>
                                <div >
                                    <a href="/deletdata/<%= val.id %>" class="btn btn-danger">Delete</a>
                                    <a href="/editpage/<%= val.id %>" class="btn btn-warning">Edit</a>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </main>
    </div>
 <!-- Bootstrap Icons -->
 <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">

 <!-- Bootstrap JS -->
 <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
