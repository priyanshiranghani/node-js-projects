<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel</title>

     <!-- Bootstrap CSS -->
     <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        /* General Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            /* background-color: #f4f6f9; */
            color: #333;
            background-image: url('../uploads/9.avif');
            background-repeat: no-repeat;
            background-size: cover;
        }

        a {
            text-decoration: none;
            color: inherit;
        }

        /* Header */
        .header {
            background: #4CAF50;
            color: white;
            padding: 1rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .header h1 {
            font-size: 1.8rem;
            font-weight: bold;
        }

        .header button {
            background-color: #ff5252;
            border: none;
            color: white;
            padding: 0.6rem 1.2rem;
            font-size: 1rem;
            border-radius: 8px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .header button:hover {
            background-color: #e53935;
        }

        /* Sidebar */
        .sidebar {
            width: 250px;
            background: #333;
            color: white;
            position: fixed;
            top: 0;
            bottom: 0;
            padding: 2rem 1rem;
            overflow-y: auto;
        }

        .sidebar h3 {
            font-size: 1.5rem;
            text-align: center;
            margin-bottom: 1.5rem;
        }

        .nav-links {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .nav-link {
            font-size: 1rem;
            padding: 0.8rem 1rem;
            background: #444;
            border-radius: 6px;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            transition: background-color 0.3s ease;
        }

        .nav-link:hover {
            background-color: #555;
        }

        .nav-link i {
            font-size: 1.2rem;
        }

        /* Main Content */
        .main-content {
            /* margin-left: 250px; */
            padding: 2rem;
        }

        .main-content h2 {
            font-size: 2rem;
            margin-bottom: 1.5rem;
        }

        /* Cards */
        .card {
    background: rgba(255, 255, 255, 0.2); /* Semi-transparent white */
    border-radius: 16px;
    box-shadow: 0 8px 32px rgba(31, 38, 135, 0.37); /* Subtle shadow */
    backdrop-filter: blur(10px); /* Frosted glass blur */
    -webkit-backdrop-filter: blur(10px); /* For Safari support */
    border: 1px solid rgba(255, 255, 255, 0.18); /* Light border */
    overflow: hidden;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    padding: 30px;
}

.card:hover {
    transform: translateY(-5px);
    box-shadow: 0 12px 24px rgba(31, 38, 135, 0.4); /* Enhance hover effect */
}

        .card img {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }

        .card-content {
            padding: 1rem;
        }

        .card-content h3 {
            font-size: 1.5rem;
            color: #4CAF50;
            margin-bottom: 0.8rem;
        }

        .card-content p {
            font-size: 0.9rem;
            color: #777;
        }

        .card-buttons {
            display: flex;
            justify-content: space-between;
            padding: 1rem;
            border-top: 1px solid #f4f4f4;
        }

        .card-buttons a {
            padding: 0.6rem 1rem;
            border-radius: 6px;
            text-align: center;
            font-size: 0.9rem;
            transition: background-color 0.3s ease;
        }

        .card-buttons .btn-edit {
            background-color: #FFD700;
            color: #333;
        }

        .card-buttons .btn-edit:hover {
            background-color: #FFC107;
        }

        .card-buttons .btn-delete {
            background-color: #FF5252;
            color: white;
        }

        .card-buttons .btn-delete:hover {
            background-color: #E53935;
        }

        .card-buttons .btn-view {
            background-color: #4CAF50;
            color: white;
        }

        .card-buttons .btn-view:hover {
            background-color: #45A049;
        }

        /* Grid */
        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 1.5rem;
        }
    </style>
</head>

<body>
   

   

    <!-- Main Content -->
     <main class="main-content">
        <h1 align="center" class="text-white">Edit blog</h1>
    <div class="card mx-auto" style="max-width: 600px; padding: 30px;  margin: 30px 0;">
        <form action="/update" method="post" enctype="multipart/form-data">
            <input type="hidden" value="<%= single.id %>" name="id">

            <div class="mb-4">
                <label for="name" class="form-label text-white">Blog Name:-</label>
                <input type="text" class="form-control" id="name" name="name" required  value="<%= single.name %>">
            </div>
            <div class="mb-4">
                <label for="description" class="form-label  text-white">Blog Description:-</label>
                <textarea class="form-control" id="description" name="description" rows="4" required> <%= single.description %></textarea>
            </div>
            <div class="mb-4">
                <label for="image" class="form-label  text-white">Upload Image:-</label>
                <input type="file" class="form-control" id="image" name="image">
                    <div class="mt-3 text-center">
                        <img src="/<%= single.image %>" alt="Current Image" class="img-fluid rounded" style="max-height: 150px;">
                    </div>
            </div>
            <div class="d-grid">
                <button type="submit" class="btn btn-primary">Submit Blog</button>
            </div>
        </form>
    </div>
</main>

 <!-- Bootstrap Icons -->
 <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">

 <!-- Bootstrap JS -->
 <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
