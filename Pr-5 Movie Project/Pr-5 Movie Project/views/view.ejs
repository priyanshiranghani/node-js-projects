<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Page</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container my-4">
        <h2 class="text-center mb-4">View Record</h2>
        <div class="row g-4">
            <% users.map((val, i) => { %>
            <div class="col-md-4">
                <div class="card h-100 shadow-sm">
                    <img src="<%= val.image %>" class="card-img-top" alt="Movie Picture" style="height: 200px; object-fit: cover;">
                    <div class="card-body">
                        <h5 class="card-title"><%= val.moviename %></h5>
                        <p class="card-text"><strong>Type:</strong> <%= val.movie %></p>
                        <p class="card-text"><strong>Intro:</strong> <%= val.movieintro %></p>
                        <p class="card-text"><strong>Price:</strong> $<%= val.movieprice %></p>
                    </div>
                    <div class="card-footer text-center">
                        <a href="/deleteRecord?id=<%= val.id %>" class="btn btn-danger btn-sm me-2">Delete</a>
                        <a href="/editRecord?id=<%= val.id %>" class="btn btn-primary btn-sm">Edit</a>
                    </div>
                </div>
            </div>
            <% }); %>
        </div>
        <div class="text-center mt-4">
            <a href="/" class="btn btn-success">Add Record</a>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>



